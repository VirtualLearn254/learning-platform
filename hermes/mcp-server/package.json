{
  "name": "@lp/hermes-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the learning-platform production substrate to Hermes profiles — beats, rendering, frames (vision-ready), review, stitch, publish.",
  "type": "module",
  "bin": { "lp-hermes-mcp": "dist/server.js" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
